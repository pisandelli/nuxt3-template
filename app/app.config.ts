/**
 * O arquivo de configuração app.config é usado para expor
 * configurações reativas na aplicação com a capacidade de
 * atualizá-la em tempo de execução dentro do life cycle ou
 * usando um plug-in nuxt e editando-o com HMR (hot-module-replacement).
 * @example
 * export default defineAppConfig({
 *   theme: {
 *     primaryColor: '#ababab'
 *   }
 * })
 * @see https://nuxt.com/docs/guide/directory-structure/app-config
 */

export default defineAppConfig({
  icon: {
    size: '24px',
  },
});
